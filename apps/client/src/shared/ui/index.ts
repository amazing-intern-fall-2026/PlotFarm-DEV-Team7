/**
 * @layer shared/ui
 * @description Atomic UI components dùng chung toàn app — FSD Layer: shared.
 *
 * Trách nhiệm:
 *  - Chứa các primitive / atom-level components KHÔNG chứa business logic
 *  - Hoàn toàn reusable, không phụ thuộc vào domain Plot Farm
 *  - Sử dụng Design Tokens từ index.css (CSS custom properties)
 *  - Ví dụ: Button, Input, Badge, Modal, Tooltip, Spinner, Avatar, Card, Divider
 *
 * Quy tắc FSD:
 *  - shared/ KHÔNG được import từ bất kỳ layer nào khác
 *  - Chỉ dùng vanilla CSS hoặc CSS Modules (theo convention dự án)
 *
 * TODO: Implement từng component theo thiết kế UI (Figma / Design System)
 */
export {};
